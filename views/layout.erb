<!DOCTYPE html>
<html lang="ru" data-bs-theme="light">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title><%= @title || "Партограмма ДАР" %></title>
  
  <!-- CSRF токен -->
  <meta name="csrf-token" content="<%= Rack::Protection::AuthenticityToken.token(session) %>">
  
  <link rel="icon" type="image/png" href="/images/favicon.png">

  <!-- Bootstrap 5 -->
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.11.0/font/bootstrap-icons.css">
  
  <!-- Основная тема -->
  <link rel="stylesheet" href="/css/theme.css">
  
  <!-- Компоненты (в порядке зависимости) -->
  <link rel="stylesheet" href="/css/components/navbar.css">
  <link rel="stylesheet" href="/css/components/cards.css">
  <link rel="stylesheet" href="/css/components/forms.css">
  <link rel="stylesheet" href="/css/components/tables.css">
  <link rel="stylesheet" href="/css/components/timers.css">
  
  <!-- Утилиты -->
  <link rel="stylesheet" href="/css/utilities.css">
  
  <!-- Адаптивность -->
  <link rel="stylesheet" href="/css/responsive/tablet.css">

</head>
<body>
  <!-- Навигация -->
  <nav class="navbar navbar-expand-lg navbar-main sticky-top">
    <div class="container">
      <a class="navbar-brand d-flex align-items-center" href="/">
        <img src="/images/logo.png" class="navbar-logo me-2">
        <span>Партограмма ДАР</span>
      </a>
      
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#mainNavbar">
        <span class="navbar-toggler-icon"></span>
      </button>
      
      <div class="collapse navbar-collapse" id="mainNavbar">
        <ul class="navbar-nav ms-auto">
          <li class="nav-item">
            <a class="nav-link <%= 'active' if request.path == '/' || request.path == '/patients' %>" 
               href="/patients">
              <i class="bi bi-people me-1"></i> Пациенты
            </a>
          </li>
          <li class="nav-item">
            <a class="nav-link <%= 'active' if request.path == '/patients/new' %>" 
               href="/patients/new">
              <i class="bi bi-plus-circle me-1"></i> Новая пациентка
            </a>
          </li>
        </ul>
      </div>
    </div>
  </nav>

  <!-- Основной контент -->
  <main class="container mt-4">
    <!-- Уведомления -->
    <% if session[:notice] %>
      <div class="alert alert-success alert-dismissible fade show" role="alert">
        <div class="d-flex align-items-center">
          <i class="bi bi-check-circle-fill me-2"></i>
          <div><%= session[:notice] %></div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
      <% session[:notice] = nil %>
    <% end %>
    
    <% if session[:alert] %>
      <div class="alert alert-danger alert-dismissible fade show" role="alert">
        <div class="d-flex align-items-center">
          <i class="bi bi-exclamation-triangle-fill me-2"></i>
          <div><%= session[:alert] %></div>
        </div>
        <button type="button" class="btn-close" data-bs-dismiss="alert"></button>
      </div>
      <% session[:alert] = nil %>
    <% end %>

    <!-- Контент страницы -->
    <div class="fade-in">
      <%= yield %>
    </div>
  </main>

  <!-- Футер -->
  <footer class="mt-5 py-4 border-top">
    <div class="container">
      <div class="row align-items-center">
        <div class="col-md-6 text-center text-md-start mb-3 mb-md-0">
          <span class="text-muted">© <%= Time.now.year %> Партограмма ДАР</span>
        </div>
        <div class="col-md-6 text-center text-md-end">
          <span class="text-muted"><small><i>by iMprov</i></small></span>
        </div>
      </div>
    </div>
  </footer>

  <!-- Скрипты -->
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
  <script src="/js/app.js"></script>
</body>
</html>